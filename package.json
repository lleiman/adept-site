{
  "name": "adept-site",
  "version": "1.0.0",
  "private": true,
  "description": "ADEPT — AI Production Studio. Portfolio site.",
  "scripts": {
    "start": "serve . -l ${PORT:-3000} --no-clipboard --no-port-switching"
  },
  "dependencies": {
    "serve": "14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
